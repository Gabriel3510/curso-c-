The sizeof operator
    Determines the size in bits of the type or variable

Exemples:
    sizeof(int)
    sizeof(siouble)

    sizeof(some_variable)

    sizeof some_variable


<climits> and <cfloat>

The climits ad cfloat include files contain size and precision infomation about your implementation of C++

    INT_MAX
    INT_MIN
    LONG_MIN
    LONG_MAX
    FLT_MIN
    FLT_MAX

