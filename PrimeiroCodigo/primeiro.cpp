#include <iostream>
//A biblioteca iostream fornece funcionalidades para entrada e saída de dados, como os objetos std::cout (para imprimir na tela) e std::cin (para ler do teclado).
 int main() {
     std::cout << "Hello, World!" << std::endl;
    //cou comando de saida de dados
    //std::cout representa o fluxo de saída padrão (console)
    //endl insere uma nova linha e
    //garante que o buffer seja esvaziado
    return 0;
    //retorna 0 para indicar que o programa terminou com sucesso
}